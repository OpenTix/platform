{
	"name": "backend",
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"projectType": "application",
	"sourceRoot": "apps/backend",
	"tags": [],
	"targets": {
		"build": {
			"executor": "nx:run-commands",
			"options": {
				"command": "mkdir -p dist/apps/backend && for f in apps/backend/*.go; do go build -o dist/apps/backend/$(basename \"$f\" .go) \"$f\"; done"
			}
		},
		"lint": {
			"executor": "nx:run-commands",
			"options": {
				"command": "for f in apps/backend/*.go; do go vet \"$f\"; done"
			}
		},
		"test": {
			"executor": "nx:run-commands",
			"options": {
				"command": "for f in apps/backend/*.go; do go test -v \"$f\"; done"
			}
		},
		"tidy": {
			"executor": "@nx-go/nx-go:tidy"
		}
	}
}
